import { ScreenConstants } from '@/styles/Global.style'
import styled from 'styled-components/native'

export const Container = styled.View`
    flex: 1;
    display: flex;
    backgroundColor: ${ScreenConstants.backgroundColor};
`